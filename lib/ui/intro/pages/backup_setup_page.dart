import 'package:flutter/material.dart';

class BackupSetupPage extends StatelessWidget {
  const BackupSetupPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(body: Center(child: Text("Backup Setup")));
  }
}

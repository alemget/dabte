# هيكل المشروع ووظائف الصفحات

## 📂 هيكل المجلدات (Project Tree)

```text
lib/
├── main.dart                      (نقطة الدخول، تهيئة التطبيق، قفل التطبيق)
├── ui/
│   ├── main_screen.dart           (الشاشة الرئيسية: تحتوي على شريط التنقل السفلي)
│   ├── lock_screen.dart           (شاشة القفل: إدخال الرمز أو البصمة)
│   │
│   ├── clients/                   (مجلد العملاء - القسم الرئيسي الأول)
│   │   ├── clients_page.dart              (صفحة قائمة العملاء)
│   │   ├── ad_edit_client_page.dart       (صفحة إضافة/تعديل عميل)
│   │   ├── client_details_page.dart       (صفحة تفاصيل العميل وكشف الحساب)
│   │   └── add_edit_transaction_page.dart (صفحة إضافة/تعديل معاملة مالية)
│   │
│   └── settings/                  (مجلد الإعدادات - القسم الرئيسي الثاني)
│       ├── settings_page.dart             (قائمة الإعدادات الرئيسية)
│       ├── personal_profile_page.dart     (صفحة الملف الشخصي للمستخدم)
│       ├── currencies_page.dart           (صفحة إدارة العملات)
│       ├── lock_settings_page.dart        (صفحة إعدادات الأمان والقفل)
│       ├── language_settings_page.dart    (صفحة تغيير اللغة)
│       ├── notifications_settings_page.dart (صفحة إعدادات الإشعارات)
│       ├── about_app_page.dart            (صفحة حول التطبيق)
│       └── backup/                        (خدمات وصفحات النسخ الاحتياطي)
│
├── data/                          (طبقة البيانات: قواعد البيانات والمستودعات)
├── models/                        (نماذج البيانات: مثل Client, Transaction)
├── providers/                     (إدارة الحالة: ClientProvider, LanguageProvider)
├── services/                      (الخدمات: الإشعارات، PDF، المشاركة)
└── l10n/                          (ملفات الترجمة واللغات)
```

---

## 📄 وظيفة كل صفحة (Page Functions)

### 1. الشاشات الأساسية (Core Screens)
*   **`main.dart`**: ليس صفحة بحد ذاته ولكنه يحتوي على **`AppLockWrapper`** الذي يقرر ما إذا كان يجب عرض شاشة القفل أو الدخول للتطبيق مباشرة، كما يقوم بتهيئة اللغات والسمات (Themes).
*   **`main_screen.dart`**: هي "الحاوية" الرئيسية للتطبيق، تحتوي على شريط التنفل السفلي (Bottom Navigation Bar) للتبديل بين قسم "العملاء" وقسم "الإعدادات".
*   **`lock_screen.dart`**: تظهر عند فتح التطبيق (إذا كان القفل مفعلاً) وتطلب من المستخدم إدخال الرمز أو البصمة للدخول.

### 2. قسم العملاء (Clients Section)
*   **`clients_page.dart`**: الصفحة الرئيسية التي تعرض قائمة جميع العملاء مع إجمالي الديون. تحتوي على ميزات البحث، الفلترة، وزر لإضافة عميل جديد.
*   **`add_edit_client_page.dart`**: نموذج لإدخال بيانات العميل الجديد (الاسم، الهاتف) أو تعديل بيانات عميل حالي.
*   **`client_details_page.dart`**: صفحة محورية تعرض كشف حساب العميل بالكامل (جميع المعاملات "له" و "عليه")، وتسمح بتوليد تقارير PDF، والمشاركة عبر واتساب، والاتصال بالعميل.
*   **`add_edit_transaction_page.dart`**: نموذج لإضافة دين جديد أو سداد دفعة، مع تحديد المبلغ، العملة، التاريخ، وإضافة ملاحظات أو صور.

### 3. قسم الإعدادات (Settings Section)
*   **`settings_page.dart`**: لوحة التحكم التي تؤدي إلى جميع صفحات الإعدادات الفرعية.
*   **`personal_profile_page.dart`**: لإعداد بيانات صاحب التطبيق (اسم الشركة/الشخص، اللوجو، معلومات التواصل) التي ستظهر في تقارير الـ PDF والمشاركات.
*   **`currencies_page.dart`**: لإضافة وتفعيل العملات التي تتعامل بها، وتحديد سعر الصرف الافتراضي.
*   **`lock_settings_page.dart`**: للتحكم في أمان التطبيق (تفعيل/إلغاء القفل، تغيير الرمز السري، تفعيل البصمة).
*   **`language_settings_page.dart`**: للتبديل بين اللغة العربية والإنجليزية.
*   **`notifications_settings_page.dart`**: لضبط تذكيرات الديون وجدولة الإشعارات.
*   **`about_app_page.dart`**: تعرض معلومات عن إصدار التطبيق وروابط التواصل مع المطور.
